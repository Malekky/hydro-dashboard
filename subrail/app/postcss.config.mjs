/**
 * Local PostCSS config. Required: postcss-load-config walks up directories and would
 * otherwise pick up the parent repo's Tailwind config, which this app doesn't use.
 */
export default { plugins: {} };
